<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".controllers.activities.GeneralMenuActivity">

    <androidx.appcompat.widget.Toolbar
            android:id="@+id/general_menu_toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="?attr/colorPrimary"
            android:elevation="4dp">

        <TextView
                android:text="@string/app_name"
                android:textSize="24sp"
                android:textColor="@color/white"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"/>

        <ImageButton
                android:background="?android:attr/selectableItemBackground"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:src="@drawable/ic_baseline_search_28"
                android:layout_gravity="right"
                android:layout_marginRight="10dp"
                android:onClick="goToSearchActivity"/>

    </androidx.appcompat.widget.Toolbar>

    <ListView
            android:id="@+id/list_of_personal_chat_history"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@+id/general_menu_toolbar"

            android:divider="@android:color/transparent"
            android:footerDividersEnabled="false"
            android:stackFromBottom="true"
            android:transcriptMode="alwaysScroll"
            android:clipToPadding="false"
            android:scrollbarStyle="outsideOverlay">
    </ListView>

</RelativeLayout>